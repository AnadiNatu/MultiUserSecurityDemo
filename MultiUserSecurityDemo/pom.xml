<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- ================= SPRING BOOT PARENT ================= -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <!-- FIX: relativePath empty tag is correct for fetching from Maven Central -->
        <relativePath/>
    </parent>

    <!-- ================= PROJECT META ================= -->
    <groupId>com.example</groupId>
    <artifactId>MultiUserSecurityDemo</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>MultiUserSecurityDemo</name>
    <description>Multi User Security Demo with JWT, Redis, Mail, Twilio</description>

    <!-- ================= PROPERTIES ================= -->
    <properties>
        <java.version>21</java.version>
        <!--
            FIX: Spring Cloud 2023.0.4 is the correct BOM version for Spring Boot 3.2.x.
            Compatibility matrix: Boot 3.2.x → Cloud 2023.0.x ✓
        -->
        <spring-cloud.version>2023.0.4</spring-cloud.version>
    </properties>

    <!-- ================= DEPENDENCIES ================= -->
    <dependencies>

        <!-- ===== CORE ===== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <!-- Version managed by Spring Boot parent - no version tag needed -->
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
            <!-- Version managed by Spring Boot parent -->
        </dependency>

        <!-- ===== VALIDATION =====
             FIX: Was MISSING entirely. Required for @NotBlank, @Email, @Size,
             @Valid on your DTOs. Without this, bean validation annotations are
             silently ignored and no input validation occurs at all.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- ===== SECURITY ===== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- ===== DATABASE =====
             FIX: No version tag needed — Boot 3.2.5 manages PostgreSQL driver
             at version 42.7.3 automatically via the parent BOM.
             Keeping <scope>runtime</scope> is correct.
        -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- ===== LOMBOK =====
             FIX: Version 1.18.34 is the correct Java 21-safe version. Keeping
             the explicit override is intentional here because Spring Boot 3.2.5
             manages Lombok at 1.18.30 internally, which has a known annotation
             processor issue on Java 21 record patterns. 1.18.34 is safer.
             <scope>provided</scope> is correct — compile-time only.
        -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.34</version>
            <scope>provided</scope>
        </dependency>

        <!-- ===== JWT =====
             jjwt 0.12.3 is the latest stable release. The three artifacts
             (api / impl / jackson) must always be on the SAME version.
             - jjwt-api: compile scope (needed at compile time for your code)
             - jjwt-impl: runtime scope (implementation, loaded by SPI at runtime)
             - jjwt-jackson: runtime scope (JSON serialization, loaded at runtime)
             This split is correct and intentional by the JJWT project design.
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.3</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.3</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.3</version>
            <scope>runtime</scope>
        </dependency>

        <!-- ===== CACHE =====
             FIX: spring-boot-starter-cache version managed by Boot parent.
             FIX: caffeine version also managed by Boot 3.2.5 parent at 3.1.8.
             REMOVED explicit version tag from caffeine — let Boot manage it.
             Boot-managed version guarantees compatibility with CaffeineCacheManager.
             The manual CacheConfig @Bean you wrote takes full control of cache
             setup, so the properties-based config is REMOVED from application.properties.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>

        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <!-- FIX: No version needed — Boot 3.2.5 manages this at 3.1.8 -->
        </dependency>

        <!-- ===== REDIS =====
             Used for: token blacklisting, OTP storage, distributed caching.
             Version managed by Boot parent.
        -->
<!--        <dependency>-->
<!--            <groupId>org.springframework.boot</groupId>-->
<!--            <artifactId>spring-boot-starter-data-redis</artifactId>-->
<!--        </dependency>-->

        <!-- ===== MAIL ===== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <!-- ===== AOP =====
             Used by your PerformanceLoggingAspect. Version managed by Boot parent.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <!-- ===== ACTUATOR =====
             Exposes /actuator/health, /actuator/metrics, /actuator/info etc.
             Version managed by Boot parent.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>

        <dependency>
            <groupId>com.twilio.sdk</groupId>
            <artifactId>twilio</artifactId>
            <version>10.1.0</version>
        </dependency>

        <dependency><groupId>org.springframework.session</groupId><artifactId>spring-session-jdbc</artifactId></dependency>

        <dependency><groupId>com.cloudinary</groupId><artifactId>cloudinary-http44</artifactId><version>1.36.0</version></dependency>
        <!-- ===== SPRING CLOUD LOAD BALANCER =====
             FIX: Version managed by Spring Cloud BOM (2023.0.4) declared below.
             No explicit version tag needed or recommended here.
             NOTE: Only keep this if you plan to use @LoadBalanced RestTemplate
             or WebClient for inter-service calls. If this is a standalone
             single-service application, you can safely remove this dependency.
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>

        <!-- ===== TWILIO =====
             FIX: 10.1.0 is the current latest stable release as of 2025. ✓
             Twilio is NOT managed by Spring Boot BOM, so explicit version is
             required and correct here.
        -->
        <dependency>
            <groupId>com.twilio.sdk</groupId>
            <artifactId>twilio</artifactId>
            <version>10.1.0</version>
        </dependency>

        <!-- ===== SPRING SESSION JDBC =====
             FIX: Version managed by Boot parent.
             IMPORTANT: You have spring-session-jdbc declared here AND
             spring-boot-starter-data-redis above. These two can conflict
             unless you explicitly set spring.session.store-type=jdbc in
             application.properties (which you have done - so this is correct).
             If you later want Redis-backed sessions, remove this dependency
             and change spring.session.store-type=redis.
        -->
        <dependency>
            <groupId>org.springframework.session</groupId>
            <artifactId>spring-session-jdbc</artifactId>
        </dependency>

        <!-- ===== TEST ===== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
            RECOMMENDED ADDITIONS (commented out - uncomment when ready):

            Spring Security Test - for testing secured endpoints:
            <dependency>
                <groupId>org.springframework.security</groupId>
                <artifactId>spring-security-test</artifactId>
                <scope>test</scope>
            </dependency>

            OpenAPI / Swagger UI - for API documentation:
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>2.5.0</version>
            </dependency>

            Micrometer Prometheus - for metrics export to Prometheus/Grafana:
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-registry-prometheus</artifactId>
            </dependency>
        -->

        <dependency>
            <groupId>org.springframework.session</groupId>
            <artifactId>spring-session-jdbc</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>
    </dependencies>

    <!-- ================= SPRING CLOUD BOM =================
         This import manages ALL Spring Cloud dependency versions in one place.
         Cloud 2023.0.4 is compatible with Spring Boot 3.2.x.
         Always use BOM import rather than specifying cloud dependency versions manually.
    -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ================= BUILD ================= -->
    <build>
        <plugins>


            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <!-- FIX: 3.13.0 is the latest stable as of 2025 vs your 3.12.1 -->
                <configuration>
                    <release>21</release>
                    <!-- FIX: Use <release> instead of <source>+<target> for Java 9+ -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.34</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- ===== SPRING BOOT MAVEN PLUGIN =====
                 FIX: Explicitly exclude Lombok from the final fat JAR.
                 Lombok is compile-time only (<scope>provided</scope>) but
                 explicitly excluding it here is a best-practice safety net.
            -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>